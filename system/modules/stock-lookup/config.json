{
  "name": "stock-lookup",
  "type": "module",
  "scope": "system",
  "version": "1.0.0",
  "runtime": "python",
  "providerType": "api",
  "capability": "stock-symbol",
  "description": "종목코드 검색 전용 — 회사명을 공식 종목코드(6자리)와 DART corp_code(8자리)로 바꿉니다. `{\"query\":\"회사명\"}` 하나면 되고 사전 검색이 필요 없습니다. **브로커 API 에 종목코드가 필요할 때 항상 먼저 부르십시오** — 기억으로 추측한 코드는 grounding 게이트가 거부합니다.",
  "aliases": [
    "종목조회",
    "종목검색"
  ],
  "tags": [
    "종목코드",
    "종목검색",
    "종목코드 조회",
    "회사명 변환",
    "종목명",
    "티커",
    "코드 확인",
    "stock code",
    "ticker",
    "lookup",
    "resolve",
    "symbol"
  ],
  "packages": [
    "requests==2.34.2"
  ],
  "secrets": [
    "DART_API_KEY"
  ],
  "input": {
    "type": "object",
    "required": [
      "query"
    ],
    "properties": {
      "query": {
        "type": "string",
        "description": "회사명(부분 일치 가능) 또는 확인할 종목코드(6자리)·DART corp_code(8자리). 예: \"LG에너지솔루션\", \"삼성전자\", \"005930\""
      },
      "limit": {
        "type": "integer",
        "description": "candidates 최대 개수 (기본 10)"
      }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "matched": {
        "type": "object",
        "description": "최적 일치 — {corp_name, stock_code(6자리, 비상장이면 빈 문자열), corp_code(8자리 DART id)}"
      },
      "candidates": {
        "type": "array",
        "description": "복수 일치 시 후보 목록 (이름 짧은 순 = 정밀 우선). 확실하지 않으면 suggest 피커로 사용자에게 확인"
      },
      "count": {
        "type": "integer",
        "description": "일치한 상장사 수"
      }
    }
  }
}
