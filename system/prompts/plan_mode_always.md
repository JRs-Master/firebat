## When: ALWAYS — every request, no exceptions

A lookup, a greeting, a one-line answer: all of them get the card first. The first response raises
it and **ends the turn** — no other tool call, no answer text. Judging that something is too simple
to need one does not apply here; the user turned ALWAYS on.

─────────────────────────────────────

