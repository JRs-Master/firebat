{
  "name": "motion",
  "type": "module",
  "runtime": "python",
  "scope": "system",
  "providerType": "compute",
  "capability": "motion-graphics",
  "version": "0.1.0",
  "description": "캔버스 코드로 굽는 2D 모션그래픽·캐릭터 애니메이션 영상 — 씬 선언(JSON)을 받아 쇼츠 mp4, 정지 스틸, 투명 클립아트 스티커 PNG 를 만든다. 배경은 그라디언트·밤하늘·image_gen 실사 이미지, 위에 캐릭터(파이어뱃)·말풍선·타이틀·정보카드·라인업·불꽃놀이·하트·켄번즈 이미지 층을 얹는다. BGM·음성 트랙 먹싱과 음성 립싱크 지원.",
  "tags": ["동영상", "영상 제작", "쇼츠", "애니메이션", "모션그래픽", "클립아트", "스티커", "홍보 영상", "인트로", "캐릭터"],
  "aliases": ["video", "animation", "shorts", "motion-graphics", "clipart"],
  "packages": ["numpy", "pillow", "imageio-ffmpeg", "soundfile"],
  "timeoutMs": 600000,
  "settings_fields": [
    {
      "key": "fontPath",
      "type": "text",
      "label": "한글 폰트 경로",
      "description": "텍스트 렌더에 쓸 TTF/TTC 경로입니다. 비워 두면 시스템에서 나눔고딕·Noto CJK·맑은고딕 순으로 찾습니다. 한글 폰트가 없으면 한글이 든 씬은 명시 에러로 거부됩니다 (Debian/Ubuntu: apt install fonts-nanum)."
    }
  ],
  "actionCatalog": {
    "envelope": "{ \"action\": \"<id>\", <params flat at top level> }",
    "actions": [
      {
        "id": "render",
        "name": "영상 렌더",
        "description": "씬 선언을 mp4 로 굽는다 — 캐릭터·텍스트·불꽃·이미지 층을 타임라인에 얹고, BGM·음성을 먹싱한다. stills 를 주면 영상 대신 지정 시각의 정지 PNG 만 뽑는다(수정 반복용 싼 검수 경로). 씬 문법·에셋 목록은 assets 액션이 전부 말한다.",
        "domain": "제작",
        "params": ["duration", "size", "fps", "background", "layers", "audio", "stills", "quality", "async"],
        "tags": ["영상", "쇼츠", "렌더", "동영상 제작"],
        "aliases": ["make-video"],
        "needs": ["motion:assets"]
      },
      {
        "id": "sticker",
        "name": "클립아트 스티커",
        "description": "클립아트를 투명 배경 PNG 로 내보낸다 — 캐릭터(포즈 지정)·하트·별·달·말풍선(문구 옵션)을 페이지·문서·PPT 에서 재사용한다.",
        "domain": "제작",
        "params": ["name", "pose", "stickerSize"],
        "tags": ["스티커", "클립아트", "투명 PNG", "캐릭터 이미지"]
      },
      {
        "id": "save_asset",
        "name": "클립아트 저장",
        "description": "캐릭터·소품을 재사용하게 저장한다 — 도형 파츠 선언(JSON)이 원본으로 남고 썸네일 PNG 가 미디어 창고(클립아트 칩)에 뜬다. 저장 후엔 씬 sprite 와 sticker 가 이름으로 참조한다. 파츠 문법은 assets 액션이 말한다.",
        "domain": "제작",
        "params": ["name", "parts", "bones"],
        "tags": ["클립아트 저장", "캐릭터 저장", "재사용", "에셋 등록"]
      },
      {
        "id": "delete_asset",
        "name": "클립아트 삭제",
        "description": "저장된 클립아트 선언을 지운다. 미디어 창고의 썸네일은 창고에서 따로 지운다.",
        "domain": "제작",
        "params": ["name"]
      },
      {
        "id": "model_info",
        "name": "3D 모델 정보",
        "desc": "Inspect a rigged .glb: clip names and durations, triangle count, height. Call before using a model3d scene layer.",
        "params": ["media"]
      },
      {
        "id": "duration",
        "name": "오디오 길이",
        "description": "미디어 창고 오디오 파일의 길이(초)를 잰다 — 대사(voices)와 말풍선·talk 액트를 타임라인에 배치하기 전에 줄마다 부른다.",
        "domain": "제작",
        "params": ["media"],
        "tags": ["오디오 길이", "대사 싱크", "타임라인"]
      },
      {
        "id": "job",
        "name": "렌더 잡 상태",
        "description": "async:true 로 접수한 렌더의 진행률을 조회한다 — 돌고 있으면 진행률·남은 시간, 끝났으면 그 응답으로 영상을 미디어 창고에 반입한다.",
        "domain": "제작",
        "params": ["id", "redeliver"],
        "tags": ["렌더 진행률", "비동기 렌더", "잡 조회"]
      },
      {
        "id": "assets",
        "name": "에셋·씬 문법 사전",
        "description": "이 모듈이 그릴 수 있는 것 전부 — 레이어 종류별 필드, 캐릭터 포즈 파라미터, 배경 종류, 캡·기본값. render 를 부르기 전에 여기부터 읽는다.",
        "domain": "제작",
        "params": [],
        "tags": ["에셋 목록", "씬 문법", "사용법"]
      },
      {
        "id": "selftest",
        "name": "셀프테스트",
        "description": "Offline regression checks — frame geometry, sprite pixel floor, sticker alpha, scene math, tiny encode, font resolution. 자체 검사",
        "params": []
      }
    ]
  },
  "input": {
    "type": "object",
    "required": ["action"],
    "properties": {
      "action": {
        "type": "string",
        "description": "Which capability to run. Call assets first for the full scene grammar."
      },
      "duration": {
        "type": "number",
        "description": "[render] Video length in seconds, 0.5 to 90. Over ~30s prefer draft quality; a long story can also ship as several clips."
      },
      "size": {
        "type": "string",
        "description": "[render] Canvas size: '1080x1920' (shorts, default), '1920x1080', or '1080x1080'."
      },
      "fps": {
        "type": "integer",
        "description": "[render] Frames per second, 10 to 30 (default 30)."
      },
      "background": {
        "type": "object",
        "description": "[render] Background layer. {kind:'night'} = built-in starry night with moon and hills (default). {kind:'studio'} = bright presenter stage with spotlight and floor — made for caster/info videos. {kind:'gradient', top:[r,g,b], bottom:[r,g,b]}. {kind:'image', media:'/user/media/....png'} = a generated or stored image, cover-cropped — pair with image_gen for photoreal backdrops. All take vignette: 0..0.6 edge darkening (0 disables)."
      },
      "layers": {
        "type": "array",
        "description": "[render] Scene layers drawn in order, each with from/to seconds. Kinds: sprite (character with enter walk/peek/pop, acts wave/talk/jump/point/pose/anim — anim plays hand-keyed 2D cartoon keypose cycles walk/run/idle/punch/dance (prefer over mocap clip for cartoon rigs), pose takes ease snap/overshoot/anticipate and squash — point taps a normalized [x,y] target with a presenter stick, pose keyframes declared bones — and clip: built-in CMU mocap walk/punch/boxing/dance or any BVH from the media store driving the bones), bubble (speech balloon, typing), title (stacked display lines), caption (subtitle pill), card (label/value info rows), list (time-table rows with color dots and a highlight tag), image (media picture with optional Ken Burns pan-zoom), fireworks, hearts, confetti, spark (impact flash), shake (camera shake), speedlines, hpbar (fighting-game health bar), model3d (a rigged 3D character .glb toon-rendered server-side — built-in robot with 13 clips or any media-store glb; clip/plays picks animations, model_info lists them). Field-level contract comes from the assets action."
      },
      "audio": {
        "type": "object",
        "description": "[render] Optional sound: {bgm, voice, voices:[{media, at}], bgmGainDb:-8}. voice = one file at t=0; voices places dialogue lines on the timeline (measure each with the duration action, then align bubble/talk windows to the same at). Voice ducks the bgm and its envelope drives the mouth of sprites whose talk act has lipsync — mouth sync is automatic."
      },
      "stills": {
        "type": "array",
        "description": "[render] Timestamps in seconds. When present, exports still PNGs at these times INSTEAD of a video — seconds instead of minutes, use it to iterate on a scene before the final render."
      },
      "quality": {
        "type": "string",
        "description": "[render] 'final' (default, 1.5x supersampled) or 'draft' (1x, roughly twice as fast) for quick previews."
      },
      "name": {
        "type": "string",
        "description": "[sticker|save_asset|delete_asset] Asset name. sticker exports any clip-art declaration — the seeds shipped with the module (firebat, heart, star, moon, balloon) or one saved with save_asset; save_asset names the new declaration (1-24 chars, letters/digits/한글/-/_)."
      },
      "media": {
        "type": "string",
        "description": "[duration] Media-store path of an audio file (/user/media/...)."
      },
      "async": {
        "type": "boolean",
        "description": "[render] true = validate, return a jobId immediately, and render in the background — REQUIRED for renders longer than ~20s (they outlive the tool roundtrip). Poll with the job action; the poll that sees it finished imports the video."
      },
      "id": {
        "type": "string",
        "description": "[job] jobId returned by render with async:true."
      },
      "redeliver": {
        "type": "boolean",
        "description": "[job] Re-import a finished video that was already delivered once (default false)."
      },
      "bones": {
        "type": "object",
        "description": "[save_asset] Optional FK bone chains {name: {pivot:[x,y], parent?}} (max 40). Parts tagged bone:'name' follow their bone; children follow parents (shoulder->elbow->fist). Drive with the pose act {do:'pose', bones:{name:degrees}} or sticker pose.bones."
      },
      "parts": {
        "type": "array",
        "description": "[save_asset] The clip art's shape parts — the ORIGINAL that scenes and stickers redraw from. 100x100 viewBox, feet at (50,100), y down. Each part: {shape: ellipse|rect|polygon|capsule|heart|star, at/size or points or ends+width, fill:[r,g,b], outline?, role: swing (waves) | mouth (opens on talk) | eye (blinks)}. shape 'image' = cutout rigging: {media, crop polygon in 0..1 image coords, at, width, pivot?, role?} turns a piece of an attached picture into a joint. Full grammar: the assets action."
      },
      "pose": {
        "type": "object",
        "description": "[sticker] Asset parameters. firebat: {wave:0..1, mouth:0..1, blink:0..1, squash:0.7..1.3, look:[x,y]}. heart/star/moon: {color:[r,g,b]}. balloon: {text}. Omit for defaults."
      },
      "stickerSize": {
        "type": "integer",
        "description": "[sticker] Longest edge in pixels, 200 to 2048 (default 900)."
      }
    }
  }
}
