{
  "name": "calendar",
  "type": "module",
  "scope": "system",
  "version": "1.0.0",
  "description": "Firebat 내장 캘린더 — 날짜와 일정을 기록합니다. 실행 기능은 없습니다.",
  "tags": [
    "캘린더",
    "달력",
    "일정",
    "일정관리",
    "약속",
    "기념일",
    "스케줄기록",
    "예정",
    "일정추가",
    "일정조회",
    "일정삭제",
    "다가오는일정",
    "calendar",
    "event"
  ],
  "aliases": [
    "캘린더",
    "일정"
  ],
  "runtime": "node",
  "capability": "calendar",
  "providerType": "local",
  "secrets": [],
  "input": {
    "type": "object",
    "required": [
      "action"
    ],
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "list-upcoming",
          "list-range",
          "add",
          "update",
          "delete",
          "find"
        ],
        "description": "list-upcoming=next N days from today (default 7), list-range=fromTm~toTm range, add=new event (id auto-generated), update=patch fields by id, delete=soft-delete by id (records deletedAt), find=search title/description/tags"
      },
      "id": {
        "type": "string",
        "description": "[update/delete] Event ID (returned by the add action)."
      },
      "title": {
        "type": "string",
        "description": "[add/update] Event title"
      },
      "startAt": {
        "type": "string",
        "description": "[add/update] Start time ISO 8601 (e.g., 2026-05-04T09:00:00+09:00)"
      },
      "zone": {
        "type": "string",
        "description": "[add/update] 이 일정이 어느 시계를 쓰나. 생략(기본) = 등록 시점 시간대로 고정 — 설정 시간대를 바꿔도 일정은 그대로이고 표시만 바뀝니다. \"auto\" = 설정 시간대를 따름 — 설정을 바꾸면 이 일정의 시각도 같이 움직입니다(\"내가 어디 있든 오전 9시\"). update 에서 \"\" 를 주면 고정으로 되돌립니다."
      },
      "endAt": {
        "type": "string",
        "description": "[add/update] End time ISO (optional). If omitted, defaults to startAt + 1 hour"
      },
      "location": {
        "type": "string",
        "description": "[add/update] Location (optional)"
      },
      "description": {
        "type": "string",
        "description": "[add/update] Detailed notes (optional). Free-form text for IPO allocation info, trading notes, etc."
      },
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "[add/update] Tags (free-form strings). Used by find filters"
      },
      "linkedJobId": {
        "type": "string",
        "description": "[add/update] Linked cron job ID — enables two-way tracking between events and cron jobs. After AI registers a cron via schedule_task, pass the returned jobId here so that updating or deleting the event also cleans up the cron"
      },
      "fromTm": {
        "type": "string",
        "description": "[list-range] Start date ISO (YYYY-MM-DD)"
      },
      "toTm": {
        "type": "string",
        "description": "[list-range] End date ISO"
      },
      "days": {
        "type": "integer",
        "default": 7,
        "description": "[list-upcoming] Next N days from today (default 7)"
      },
      "query": {
        "type": "string",
        "description": "[find] Search query (full-text over title/description/tags)"
      },
      "tag": {
        "type": "string",
        "description": "[find/list-*] Tag filter"
      },
      "includeDeleted": {
        "type": "boolean",
        "default": false,
        "description": "Whether to include soft-deleted events"
      },
      "limit": {
        "type": "integer",
        "default": 50,
        "description": "Maximum number of results"
      }
    },
    "additionalProperties": false
  },
  "output": {
    "type": "object",
    "properties": {
      "items": {
        "type": "array",
        "description": "Results of list-* / find (id, title, startAt, endAt, tags, linkedJobId, ...)"
      },
      "event": {
        "type": "object",
        "description": "Single event returned by add/update"
      },
      "deleted": {
        "type": "boolean",
        "description": "Result of delete"
      }
    }
  }
}
