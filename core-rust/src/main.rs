//! Firebat Core — gRPC server entry (self-hosted distribution, Phase C 활성).
//!
//! Phase A (현재) — stub only. Phase B 에서 gRPC server + 21 매니저 + 어댑터 박힘.

fn main() {
    println!("Firebat Core v{} — Phase A stub (gRPC server activated in Phase B)", firebat_core::version());
}
